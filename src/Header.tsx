import * as React from 'react';

const Header: React.FC = () => {
    return (
        <header className="App-header">
            <a href="/Resume" target="_black"> test</a>
        </header>
    );
};

export default Header;